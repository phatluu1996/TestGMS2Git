script_execute(BossInitVariables,0,0,0,0,0);
/// Bit Individual Init Variables

dashspeed=10; // Dash speed (if it gets too fast to decrease)
jumpspeed=6; // Jump speed (if it gets too fast to decrease)

