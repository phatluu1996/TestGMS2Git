function EnemyOutside() {
	// Check if it is outside or inside the view.
	// Verify if this object is in activity
	// Destroy if the two variables is true

	if (outside_view==true) and inActive == true 
	{ 
	    instance_destroy();
	}


}
